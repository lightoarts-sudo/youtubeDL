{
  "name": "youtube-clipper",
  "version": "1.0.0",
  "private": true,
  "description": "Preview a YouTube video and download a selected clip.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
